import { Typography, Button } from "@mui/material"

function Sales() {
    return <>
        <Typography>Ventes</Typography>
        <Button variant="contained">Créer une vente</Button>
    </>

}

export default Sales